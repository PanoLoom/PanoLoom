name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  rust:
    name: Engine (native)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt --all --check
      - run: cargo clippy --workspace --all-targets -- -D warnings
      - run: cargo test --workspace

  web:
    name: Wasm build + app
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # nightly + rust-src for the multithreaded build (build-std, +atomics)
      - uses: dtolnay/rust-toolchain@nightly
        with:
          targets: wasm32-unknown-unknown
          components: rust-src
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build:wasm
      - run: pnpm build:wasm-mt
      - run: pnpm typecheck
      - run: pnpm --filter @panoloom/metadata test
      - run: pnpm --filter @panoloom/shared test
      - run: pnpm --filter @panoloom/app build
      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium
      - name: Browser e2e (bundled sample set)
        run: |
          pnpm --filter @panoloom/app exec vite preview --port 4173 &
          for i in $(seq 1 30); do curl -sf http://localhost:4173 > /dev/null && break; sleep 1; done
          node packages/app/e2e/m5-stitch.mjs
          node packages/app/e2e/m8-sample.mjs
          node packages/app/e2e/m8-project.mjs
          node packages/app/e2e/m7-export.mjs
      - name: Report wasm size
        run: ls -la packages/app/src/engine/pkg/*.wasm packages/app/src/engine/pkg-mt/*.wasm && gzip -k -9 packages/app/src/engine/pkg/panoloom_bg.wasm packages/app/src/engine/pkg-mt/panoloom_bg.wasm && ls -la packages/app/src/engine/pkg/*.gz packages/app/src/engine/pkg-mt/*.gz
